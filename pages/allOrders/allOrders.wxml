<view class="container">
  <view wx:for="{{orders}}" wx:key="{{index}}" bindtouchend="navToDetail" data-index="{{index}}">
    <block wx:for="{{orders[index]}}">
      {{item}}
    </block>
  </view>
</view>